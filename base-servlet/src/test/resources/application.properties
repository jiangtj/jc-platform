spring.application.name=base-servlet
server.port=17001

# management config
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*

auth.filter=true
auth.exclude-patterns=/,/insecure/**
