spring:
  application:
    name: system-server
  cloud:
    polaris:
      address: grpc://localhost:18091
      namespace: default
      config:
        address: grpc://localhost:18093
        auto-refresh: true
  config:
    import:
      - optional:polaris:dev:system.properties
      - optional:polaris:dev:common.properties