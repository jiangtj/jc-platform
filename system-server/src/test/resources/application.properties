spring.application.name=base-reactive
server.port=17080

# management config
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*

auth.filter=true
auth.exclude-patterns=/,/insecure/**
